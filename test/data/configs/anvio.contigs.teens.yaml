samples_name: "teens"
samples_pattern:
    glob: "../../data/raw_reads/*_1-1?_DNA*.fastq"
    re: "/[^A-Za-z]*([A-Za-z][^/]*)_S\\d[^/]+\\.fastq"
    cleaned: True
contigs_fasta: ../../data/contigs/contigs.aloha.fa
min_contig_length: 500
run_cogs: False
skip_contig_clustering: False
sample_tree:
    coverage_metrics:
        - mean_coverage_Q2Q3_contigs
        - mean_coverage_contigs
        - variability_contigs
        - std_coverage_contigs
        - abundance_contigs
        - detection_contigs
threads:
    bwa: 10
    anvio-profile: 1
anvio_env: anvio

